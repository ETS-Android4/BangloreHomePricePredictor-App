dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        google()  // Google's Maven repository
    }
}
rootProject.name = "BangloreHomePricePredictor"
include ':app'
